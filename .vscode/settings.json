{
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "eslint.validate": ["javascript", "javascriptreact", "typescript", "typescriptreact"],
  "prettier.requireConfig": true, // Ensure Prettier uses the configuration file
  "prettier.tabWidth": 2, // Ensure Prettier uses the same tab width
  "prettier.useTabs": false // Ensure Prettier uses spaces instead of tabs
}
